version: '3.8'

services:
  web:
    build: .
    container_name: holding-website
    ports:
      - "3124:80"
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge

